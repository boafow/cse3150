To build an executable run the following command
$ make test-gv

All feature for part 1 have been implemented
-Insert Mode
--Draw rectangle
--Scale rectangle as it is drawn
--Defaults to insert mode on execution

-Edit Mode
--Blue outline
--Move
--Added to command queue

Undo/Redo
--Undo
--Redo
--Clear command queue when new action is taken

One known bug is the the highlight of the shape when it is selected for moving is delayed
